#include <stdio.h>
int main(){
    int i, j, k;
    scanf("%d", &n);
    int arr[n];
    for(i = 0; i < n; i++){
        scanf("%d", &arr[n]);

    }

    for(i = 0; i < n; i++){
        if(arr[i] % 2 == 0){
            j++;
        }
        else{
            k++;
        }
    }
    printf("%d %d\n", j, k)
}















